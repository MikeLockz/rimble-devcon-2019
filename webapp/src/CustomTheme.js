import { theme as rimbleTheme } from "rimble-ui";

const CustomTheme = {
  ...rimbleTheme,
  colors: {
    ...rimbleTheme.colors,
    primary: "#4E174F",
    background: "#fffff8"
  }
};

export default CustomTheme;
